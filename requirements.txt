# Exact pins, matching the locally validated .venv (main:app imports and runs
# with these) — so a Render build installs precisely what was tested here.
# To upgrade deliberately: pip install -U -r requirements.txt, retest, re-freeze.
fastapi==0.139.0
uvicorn[standard]==0.49.0
sse-starlette==1.8.2
python-jose[cryptography]==3.5.0
databases[aiosqlite]==0.9.0
python-dotenv==1.2.2
httpx==0.28.1
bcrypt==4.3.0
paho-mqtt==2.1.0
